export enum Tool {
    PEN = "pen",
    ERASER = "eraser"
}

export enum GlobalCompositeOperation {
    DESTINATION_OUT = "destination-out",
    SOURCE_OVER = "source-over"
}

export enum LineCap {
    ROUND = "round"
}